spring:
  application:
    name: MSTxFleet-DBS-Postgis

  cloud:
    discovery:
      enabled: true
  datasource:
    username: ilisi
    password: ilisi
    url: jdbc:postgresql://localhost:5432/ilisi2023-2024
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 0